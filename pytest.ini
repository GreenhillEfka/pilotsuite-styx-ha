[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    unit: Unit tests without HA dependency
    integration: Integration tests with HA framework
    asyncio: mark test as async
filterwarnings =
    ignore::DeprecationWarning