name: CI (pilotsuite-styx-ha)

on:
  push:
    branches: [main, dev]
  pull_request:

jobs:
  ci:
    uses: ./.github/workflows/pilotsuite-dev/github-action-shared.yml
    with:
      repo-name: "pilotsuite-styx-ha"
      repo-path: "pilotsuite-dev/pilotsuite-styx-ha"
