# 2026-02-08

## Status
- Core reachable again: /health returns ok + port=8909.
- Core capabilities supported (HTTP 200).
- HA integration OptionsFlow menu labels were missing (only arrows) due to missing `options.*` translations; fixed via integration v0.2.5.

## Plan (streams)
- Main path: install v0.2.5 + HA restart → create Habitus zone `wohnbereich` via wizard → enable events forwarder → verify /api/v1/events ingestion.
- Background streams requested by user: continue work on Brain Graph (N4), PilotSuite dashboard expansion, Habitus zones bulk/paste editor (YAML/JSON) beyond wizard.

## 2026-02-08 — Prozess
- User ist für eine Weile weg; Rückfragen sammeln in `notes/questions_for_user_next_time.md`.
- Wenn ein Workstream blockiert ist (Entscheidung nötig): pausieren und an anderem Modul weiterarbeiten.
- Module-Kernel-Orchestrierung gestartet (max 2 Profiworker parallel); keine Pushes/Merges/Tags/Releases ohne explizites OK.
